plugins {
    id "org.gradle.java.experimental-jigsaw" version "0.1.1"
}

javaModule.name = 'meetup.hello'

dependencies {
    compile project(':api')
    compile 'com.athaydes.protobuf:protobuf-tcp-rpc:0.2.1'
}
